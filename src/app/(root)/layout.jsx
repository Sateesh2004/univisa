import React from 'react'
export const metadata = {
    title:"UniViza • Home",
    icons: {
      icon: "/favicon.png",
    },
  };
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
